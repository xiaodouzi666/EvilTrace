# Demo Video

Video status: recording-ready; public upload URL is pending.

Recommended title: `EvilTrace FIND EVIL Demo`.

The recording should be under 5 minutes and follow `docs/demo-script.md`.

Required moments to show:

- live terminal execution
- real local case data when available
- self-correction event in `artifacts/logs/<case>.agent.jsonl`
- provenance from final finding to artifact and audit ID
- architecture and compliance map

After recording and uploading, add the public YouTube or Vimeo URL here:

```text
TODO: add public demo video URL after upload
```

